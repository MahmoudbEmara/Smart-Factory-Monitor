/**
 * Entry Point
 * 
 * Expo app entry point - Expo handles this automatically
 * This file is kept for reference but Expo uses app.json configuration
 */

import 'expo/build/Expo.fx';
import { registerRootComponent } from 'expo';
import App from './App';

// Register the root component with Expo
registerRootComponent(App);

